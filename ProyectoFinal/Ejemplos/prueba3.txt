CREATE SERVER S
    FOREIGN DATA WRAPPER MYSQL
    OPTIONS (HOST '195.000.200.152',USER 'cesar');

ALTER SERVER S
    OPTIONS (HOST '213.145.135.132');

CREATE TRIGGER TRIG
    BEFORE UPDATE ON TABLE2 FOR EACH ROW
    SELECT * FROM CASA

CREATE VIEW VIS AS SELECT * FROM T;


DROP TRIGGER TRIG
DROP VIEW VIS,VISA
DROP SERVER IF EXISTS S