PREPARE PROP FROM "SELECT * FROM Tab"

EXECUTE STAT USING @NOMBRE,@DIR

DEALLOCATE PREPARE STAT

CREATE TABLE VENDEDORES (
  `ID_CIUDAD` INT(11) NOT NULL,
  `ID_CONCESIONARIO` INT(11) NOT NULL,
  `ID_VENDEDOR` VARCHAR(20) COLLATE LATIN1_GENERAL_CI NOT NULL,
  `NOM_VENDEDOR` VARCHAR(100) COLLATE LATIN1_GENERAL_CI NOT NULL,
  PRIMARY KEY (`ID_VENDEDOR`)
);

ALTER TABLE VENDEDORES AUTO_INCREMENT = 10;

INSERT INTO VENDEDORES(ID_CIUDAD,ID_CONCESIONARIO,ID_VENDEDOR,NOM_VENDEDOR) VALUES (1, 1, 1, 'default')

TRUNCATE TABLE VENDEDORES

DROP TABLE VENDEDORES