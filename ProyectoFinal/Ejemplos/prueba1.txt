CREATE DATABASE IF NOT EXISTS CASA

USE CASA
ALTER DATABASE CASA DEFAULT CHARSET = ASCII

CREATE TABLE TABLA2
(
   ID_FAV_DEST          INT NOT NULL,
   ID_USER              INT,
   ID_USER_FAV          INT NOT NULL,
   FAV_LATITUDE         VARCHAR(50) NOT NULL,
   FAV_LONGITUDE        VARCHAR(50) NOT NULL,
   FAV_ADDRESS          VARCHAR(50) NOT NULL,
   DATETIME_CREATION_FAV DATE NOT NULL,
   NAME_FD              VARCHAR(50) NOT NULL,
   PRIMARY KEY (ID_FAV_DEST)
);

SELECT * FROM  TABLA2
SELECT ID_USER ,FAV_ADDRESS FROM TABLA2

CREATE UNIQUE INDEX INDUSER ON TABLA2 (ID_USER(10))

DELETE FROM T1 WHERE USER="CESAR"
DROP DATABASE IF EXISTS CASA
DROP  INDEX INDUSER ON TABLA2
