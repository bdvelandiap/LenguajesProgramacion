CREATE DATABASE IF NOT EXISTS CASA
ALTER DATABASE CASA DEFAULT CHARSET = ASCII

SELECT * FROM  TABLA2
SELECT PRECIO,COSTO FROM TABLA2

CREATE UNIQUE INDEX NAMEOF ON CASA (NAME(10))



CREATE SERVER S
    FOREIGN DATA WRAPPER MYSQL
    OPTIONS (HOST '195.512.263.152',USER 'cesar');

ALTER SERVER S
    OPTIONS (HOST '213.145.135.132');

CREATE TRIGGER TRIG
BEFORE UPDATE ON CASA FOR EACH ROW
SELECT * FROM CASA

CREATE VIEW VIS AS SELECT * FROM T;

CREATE TABLE TABLA2
(
   ID_FAV_DEST          INT NOT NULL,
   ID_USER              INT,
   ID_USER_FAV          INT NOT NULL,
   FAV_LATITUDE         VARCHAR(50) NOT NULL,
   FAV_LONGITUDE        VARCHAR(50) NOT NULL,
   FAV_ADDRESS          VARCHAR(50) NOT NULL,
   DATETIME_CREATION_FAV DATE NOT NULL,
   NAME_FD              VARCHAR(50) NOT NULL,
   PRIMARY KEY (ID_FAV_DEST)
);
DROP DATABASE IF EXISTS CASA
DROP  INDEX NAMEOF ON CASA
DROP SERVER IF EXISTS S
DROP TRIGGER TRIG
DROP VIEW VIS,VISA