INSERT INTO CASA VALUES(1)
DELETE FROM T1 WHERE USER="CESAR"

LOAD XML  INFILE "person.xml" INTO TABLE  GENTE

CREATE TABLE VEHICLE_TYPE
(
   ID_VEHICLE_TYPE      INT NOT NULL,
   VEHICLE_TYPE_NAME    VARCHAR(50) NOT NULL,
   PRIMARY KEY (ID_VEHICLE_TYPE)
);

INSERT INTO ASD VALUES(1,"ASD",1)
SELECT * FROM CASAS
CREATE TABLE VEHICLE_TYPE
(
   ID_VEHICLE_TYPE      INT NOT NULL,
   VEHICLE_TYPE_NAME    VARCHAR(50) NOT NULL,
   PRIMARY KEY (ID_VEHICLE_TYPE)
);
INSERT INTO ASD VALUES("ASDASD","ASD",1)
SELECT ASD FROM ASD

